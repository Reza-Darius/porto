# services:
#     pebble:
#         image: ghcr.io/letsencrypt/pebble:latest
#         command: -config test/config/pebble-config.json -strict -dnsserver 10.30.50.3:8053
#         ports:
#             - 14000:14000 # HTTPS ACME API
#             - 15000:15000 # HTTPS Management API
#         networks:
#             acmenet:
#                 ipv4_address: 10.30.50.2
#     challtestsrv:
#         image: ghcr.io/letsencrypt/pebble-challtestsrv:latest
#         # default ipv4 is the key line, it tells the challtestsrv to resolve any DNS to this ip
#         command: -defaultIPv6 "" -defaultIPv4 172.17.0.1
#         ports:
#             - 8055:8055 # HTTP Management API
#         networks:
#             acmenet:
#                 ipv4_address: 10.30.50.3
# networks:
#     acmenet:
#         driver: bridge
#         ipam:
#             driver: default
#             config:
#                 - subnet: 10.30.50.0/24

services:
  pebble:
    image: ghcr.io/letsencrypt/pebble:latest
    command: -config test/config/pebble-config.json -strict
    ports:
      - 14000:14000 # HTTPS ACME API
      - 15000:15000 # HTTPS Management API
    extra_hosts:
      - "acmetest.com:172.17.0.1"
